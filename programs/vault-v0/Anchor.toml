[features]
seeds = false
skip-lint = false

[programs.localnet]
murk_vault_manager = "4ccQd4R1Z8yrb7am6qHyB6TPAwMD2Gp4cxhz1ZShHoQR"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "devnet-wallet.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
client = "yarn run ts-node client/*.ts"
