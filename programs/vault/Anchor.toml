[toolchain]

[features]
seeds = false
skip-lint = false

[programs.localnet]
murkfi = "9qygbwmh55Af8efyfZxs2wE4iNwhbf5ac6xtcTV1QTwB"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "./devnet-wallet.json"

[scripts]
# test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/deposit.ts"
client = "yarn run ts-node app/*.ts"
